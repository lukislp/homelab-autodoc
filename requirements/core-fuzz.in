# Fuzz harness for core (core/fuzz/), Linux only - the atheris wheel does not exist for
# Windows. Runtime lock as constraint; compiled with hashes like every other file here:
#   docker run --rm -v "$PWD:/w" -w /w python:3.12-slim sh requirements/compile.sh
-c core.txt
atheris>=2.3
