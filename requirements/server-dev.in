# CI tooling for server: runtime lock as constraint plus the [dev] extra of server/pyproject.toml.
-c server.txt
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
# Starlette 1.6 deprecates its TestClient over httpx and warns on every test run until httpx2
# is installed.
httpx2>=2.13.0
