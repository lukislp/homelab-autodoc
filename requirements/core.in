# Third-party dependencies of core, mirrored from core/pyproject.toml (keep both in sync).
# requirements/core.txt is generated from this file with hashes - never edit the lock by hand:
#   pip-compile --generate-hashes --strip-extras --allow-unsafe requirements/core.in

PyYAML>=6.0.1
