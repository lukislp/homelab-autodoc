# Third-party dependencies of collector, mirrored from collector/pyproject.toml (keep both in sync).
# requirements/collector.txt is generated from this file with hashes - never edit the lock by hand:
#   pip-compile --generate-hashes --strip-extras --allow-unsafe requirements/collector.in

-r core.in
kubernetes>=30.1.0
httpx>=0.27.0
